import React from "react";
import { Typography } from "@mui/material";

const Administrator: React.FC = () => {
  return <Typography variant="body1">Administrator</Typography>;
};

export default Administrator;
